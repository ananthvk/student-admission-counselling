[build-system]
build-backend = 'mesonpy'
requires = ['meson-python']

[project]
name = 'pymatcher'
version = '0.0.1'
description = 'Implementation of Gale-Shapley algorithm in C++, exported as a Cython module'
requires-python = '>=3.8'
authors = [
  {name = 'Ananthanarayanan Venkitakrishnan'},
]
